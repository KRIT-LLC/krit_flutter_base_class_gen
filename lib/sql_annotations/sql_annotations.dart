class Entity {
  const Entity();
}

const entity = Entity();

class PrimaryKey {
  const PrimaryKey();
}

const primaryKey = PrimaryKey();

class Searchable {
  const Searchable();
}

const searchable = Searchable();

class DeleteBy {
  const DeleteBy();
}

const deleteBy = DeleteBy();
